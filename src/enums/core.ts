enum VARIANT_ENUMS {
  DEFAULT = 'default',
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  SUCCESS = 'success',
  WARNING = 'warning',
  ERROR = 'error',
}

enum SIZE_ENUMS {
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
}

export {SIZE_ENUMS, VARIANT_ENUMS}
